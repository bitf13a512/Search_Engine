@{
    ViewBag.Title = "first";
}

<div class="section group">
    @foreach (var i in Model)
    {




        @*@foreach(var b in Model){


            if (h % 5 != 0)
            {

        *@

        <div class="grid_1_of_4 images_1_of_4">
            <a href="~/Home/preview"> <img src="@Url.Content(i.pic)" width="300" /></a>
            <h2>Lorem Ipsum is simply </h2>
            <div class="price-details">
                <div class="price-number">
                    <p><span class="rupees">$899.75</span></p>
                </div>

                @if (Session["username"] != null)
                {
                    <div class="add-cart">
                        <h4><a href="~/Home/preview">Add to Cart</a></h4>
                    </div>
                    <div class="clear"></div>
                }
                else
                {
                    <div class="add-cart">
                        <h4><a href="~/Home/login">Login to buy</a></h4>
                    </div>
                    <div class="clear"></div>
                }


            </div>

        </div>

        //}
        @*else
            {
            count = count - h;
            continue;
            }*@
        //h++;

    }










    @*<a href="~/Home/preview"> <img src="@Url.Content(i.pic)" width="300" /></a>*@



    @* } *@
    @* }*@
</div>
    
